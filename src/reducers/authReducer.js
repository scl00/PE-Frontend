const initialState = {
  checking: true, 
  // uid: null, 
  // nombre: null
}

export const authReducer = ( state= initialState, action) => {
  switch ( action.type ) {
    default:
      return state;
  }
}